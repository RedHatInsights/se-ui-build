import { FunctionComponent, ReactNode } from "react";
interface AppLayoutProps {
    children: ReactNode;
    mobileView?: boolean;
}
export declare const AppLayout: FunctionComponent<AppLayoutProps>;
export {};
